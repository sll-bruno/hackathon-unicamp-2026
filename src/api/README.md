# Backend e dados — pessoa C

Entrada: `app/main.py` (FastAPI). Pastas reservadas para rotas, modelos, schemas, serviços, configuração, aderência e efetividade.

Apenas `GET /api/health` está implementado. Banco, uploads, endpoints e integrações ficam para a frente desenvolver. Consuma a engine pelo pacote `decision_engine` e os contratos pelo pacote `contracts`.

Execução na raiz: `.venv/bin/uvicorn app.main:app --reload`.
