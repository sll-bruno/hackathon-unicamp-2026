# Vazio no desenvolvimento: o Vite encaminha /api para localhost:8000.
# Na Vercel, use a origem pública da Railway, sem barra final.
VITE_API_URL=
# A integração real é o padrão; use true somente para demonstração offline.
VITE_USE_MOCKS=false
